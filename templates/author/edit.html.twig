<h1>Edit Author</h1>

    {{ form_start(form) }}

    {{ form_row(form.username) }}
    {{ form_row(form.email) }}

    <button type="submit" class="btn btn-primary">Save Changes</button>

    {{ form_end(form) }}
    <a href="{{ path('author_list') }}" class="btn btn-secondary">Back to Authors List</a>